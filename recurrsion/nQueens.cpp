#include <iostream>
#include <vector>
using namespace std;
void placeQueen(vector<vector<int>> &board, vector<vector<int>> &result, int n) {
    int n = board.size();
    int col = 0;
    for (int i=0; i<n; i++) {
        
    }
}

vector<vector<int>> nQueens(int n) {
    vector<vector<int>> board(n, vector<int>(n,0));
    vector<vector<int>> result;

}
int main() {

    return 0;
}